---
description: Aggressive project-wide comment cleanup campaign, orchestrated zone by zone with coverage tracking
---

Invoke the doc-cleanup skill in **project** mode.

Read `references/doctrine.md` and `references/orchestration.md` first, then follow "Mode : project" (`references/mode-project.md`) in the skill. The campaign asks for one go-ahead, then runs autonomously zone by zone (serialized sub-agents), leaving all changes uncommitted.
